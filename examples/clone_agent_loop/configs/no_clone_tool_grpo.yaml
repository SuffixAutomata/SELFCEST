# Example PPO config wired for ToolAgentLoop clone rollouts.
# Point --config-path to this directory and --config-name to this file when
# running `python -m verl.trainer.main_ppo`.

hydra:
  searchpath:
    - file://<HOME_DIR>/verl/verl/trainer/config

defaults:
  - ppo_trainer
  - _self_

data:
  tokenizer: null
  train_files: <HOME_DIR>/arith/train.parquet
  val_files: <HOME_DIR>/arith/test.parquet
  train_batch_size: 32
  val_batch_size: null
  max_prompt_length: 2048
  max_response_length: 4096
  prompt_key: prompt
  reward_fn_key: data_source
  return_raw_chat: True
  return_raw_input_ids: False
  filter_overlong_prompts: True
  truncation: error
  apply_chat_template_kwargs: {}
  # Tool schemas are loaded by ToolAgentLoop itself; keep this null unless you
  # also want external tools beyond spawn_clone.
  tool_config_path: null

actor_rollout_ref:
  hybrid_engine: True

  model:
    path: /path/to/model
    custom_chat_template: null
    override_config: {}
    enable_gradient_checkpointing: True
    use_remove_padding: False

  actor:
    strategy: fsdp
    ppo_mini_batch_size: 8
    ppo_micro_batch_size_per_gpu: 1
    grad_clip: 1.0
    clip_ratio: 0.1 # normally 0.2
    entropy_coeff: 0.0
    use_dynamic_bsz: False
    use_kl_loss: True
    kl_loss_coef: 5e-4 # normally 1e-3
    optim:
      lr: 6e-7
      lr_warmup_steps: 10
      lr_scheduler_type: constant
      total_training_steps: -1  # filled at runtime
    fsdp_config:
      param_offload: False
      optimizer_offload: False
      wrap_policy:
        min_num_params: 0

  ref:
    log_prob_micro_batch_size_per_gpu: 1
    fsdp_config:
      param_offload: True
      wrap_policy:
        min_num_params: 0

  rollout:
    name: vllm
    prompt_length: ${data.max_prompt_length}
    response_length: ${data.max_response_length}
    n: 4
    do_sample: True
    tensor_model_parallel_size: 1
    data_parallel_size: 1
    pipeline_model_parallel_size: 1
    gpu_memory_utilization: 0.6
    log_prob_micro_batch_size_per_gpu: 1
    free_cache_engine: True

    # Multi-turn + clone tool settings consumed by ToolAgentLoop
    multi_turn:
      enable: False

    agent:
      num_workers: 1
      default_agent_loop: tool_agent
      agent_loop_config_path: null

reward_model:
  enable: False
  enable_resource_pool: False
  use_reward_loop: False

trainer:
  project_name: no_clone
  experiment_name: no_clone
  n_gpus_per_node: 8
  nnodes: 1
  total_epochs: 1
  save_freq: 200
  test_freq: 50
  logger: tensorboard
  debug_agentic_reward_mask_metrics: True
  debug_agentic_clone_eval_metrics: True

algorithm:
  adv_estimator: grpo
  use_kl_in_reward: False
  gamma: 1.0
  lam: 1.0
